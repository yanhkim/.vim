yanhkim's .vimrc files
======================

Download and link configuration file:
<pre>
$ git clone git://github.com/yanhkim/.vim.git ~/.vim
$ ln -s ~/.vim/vimrc ~/.vimrc
$ cd ~/.vim
$ vim +PlugInstall +qall
</pre>
